import { keyEvent } from './types/key-event';

export const KEYPRESS: keyEvent = 'keypress';
export const KEYUP: keyEvent = 'keyup';
export const KEYDOWN: keyEvent = 'keydown';
export const SHIFT = 'shift';
